# --- secrets: NEVER commit HA tokens or runtime creds ---
.ha-runtime/
token*
*.token
.env
.env.*
# The live token lands here at cast time; only the .example is tracked.
*.env
!*.env.example
secrets.*

# --- python caches / build ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# --- virtualenvs ---
.venv/
venv/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# NOTE: casting.yaml.lock is intentionally NOT ignored — it must be committed
# for bit-identical replicable installs. Do not add *.lock here.

tools/video/_work/
sidecar.log
docs/_*.png
docs/video/assets/
_build/
